# Server Configuration
PORT=3000

# Database (SQLite via Prisma)
DATABASE_URL="postgresql://user:password@localhost:5432/scrapesafe"

# Web3.storage for IPFS pinning (optional - will mock if not present)
WEB3_STORAGE_TOKEN=

# Server signing key for license receipts (REQUIRED)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Or use ethers: ethers.Wallet.createRandom().privateKey
WALLET_PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# Blockchain RPC URL (optional - for future on-chain integration)
CHAIN_RPC_URL=

# Story Protocol SDK Key (optional - will simulate if not present)
STORY_SDK_KEY=
